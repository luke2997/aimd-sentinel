# RouteGuard-Med benchmark

`routeguard_med_300.csv` is a template-curated starter benchmark for routing evaluation.

Each row labels whether the query should be routed to:

- answer
- retrieve
- clarify
- abstain
- escalate

It also includes gold source IDs and action utilities. This file is intended as a reproducible prototype benchmark, not a final clinical validation dataset.
